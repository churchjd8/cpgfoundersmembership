[project]
name = "jeff-brain"
version = "0.1.0"
description = "MCP brain for Jeff Church / CPG Founders Group"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=1.2",
    "pypdf>=6.10.2",
    "python-frontmatter>=1.1.0",
    "uvicorn>=0.30",
]
